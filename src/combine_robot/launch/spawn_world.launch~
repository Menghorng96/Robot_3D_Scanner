<!-- Spawn 1 robot in the environment -->
<launch>
    <!-- Spawn the world -->
    <include file="$(find gazebo_ros)/launch/empty_world.launch">
    </include>
     <node name="gazebo" pkg="gazebo_ros" type="gazebo" 
   args="$(find combine_robot)/worlds/test.world" respawn="false" output="screen" />

</launch>
